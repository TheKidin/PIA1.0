<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PIA1._0.EditarStockWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PIA1._0"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
        Title="EditarStockWindow">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Editar Stock" 
                       FontSize="20" 
                       FontWeight="Bold"/>
            <TextBlock x:Name="txtNombreProducto" 
                       FontSize="14" 
                       Foreground="#4A5568"
                       Margin="0,5,0,0"/>
            <TextBlock x:Name="txtPlataforma" 
                       FontSize="12" 
                       Foreground="#718096"/>
        </StackPanel>

        <!-- Contenido -->
        <StackPanel Grid.Row="1" Spacing="15">
            <NumberBox x:Name="numNuevaCantidad" 
                       Header="Nueva Cantidad en Stock" 
                       Minimum="0" 
                       Value="0"
                       SpinButtonPlacementMode="Inline"/>

            <Border Background="#F7FAFC" CornerRadius="8" Padding="15">
                <StackPanel Spacing="8">
                    <TextBlock Text="Información de Stock" 
                               FontWeight="SemiBold"
                               Foreground="#4A5568"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Stock Mínimo" 
                                       FontSize="12" 
                                       Foreground="#718096"/>
                            <TextBlock x:Name="txtStockMinimo" 
                                       FontWeight="Bold"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Stock Máximo" 
                                       FontSize="12" 
                                       Foreground="#718096"/>
                            <TextBlock x:Name="txtStockMaximo" 
                                       FontWeight="Bold"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <TextBlock x:Name="txtMensajeError" 
                       Foreground="Red" 
                       TextWrapping="Wrap"
                       Visibility="Collapsed"/>
        </StackPanel>

        <!-- Botones -->
        <Grid Grid.Row="2" ColumnSpacing="10" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="btnCancelar" 
                    Content="Cancelar" 
                    Click="BtnCancelar_Click"
                    Grid.Column="0"
                    Background="Transparent"
                    BorderBrush="#E2E8F0"
                    Foreground="#4A5568"/>

            <Button x:Name="btnActualizar" 
                    Content="Actualizar Stock" 
                    Click="BtnActualizar_Click"
                    Grid.Column="1"
                    Background="#4299E1"
                    Foreground="White"
                    BorderThickness="0"/>
        </Grid>
    </Grid>
</Window>

